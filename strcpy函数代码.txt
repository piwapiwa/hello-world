char* StrCpy(char* src)
{
    int len;
    char* array;
    len = strlen(src);
    array = (char*)malloc(sizeof(len + 1));
    for (int i = 0; i < len; i++)
    {
        array[i] = src[i];
    }
    array[len] = '\0';
    return str;
}
